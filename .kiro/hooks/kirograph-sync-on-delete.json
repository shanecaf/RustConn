{
  "name": "KiroGraph: Sync on delete",
  "version": "1.0.0",
  "description": "Immediately syncs the KiroGraph index when a source file is deleted",
  "when": {
    "type": "fileDeleted",
    "patterns": ["*.rs"]
  },
  "then": {
    "type": "runCommand",
    "command": "kirograph sync-if-dirty"
  }
}
