{
  "name": "KiroGraph: Mark dirty on save",
  "version": "1.0.0",
  "description": "Marks the KiroGraph index as dirty when a source file is saved, for deferred sync",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.rs"]
  },
  "then": {
    "type": "runCommand",
    "command": "kirograph mark-dirty"
  }
}
