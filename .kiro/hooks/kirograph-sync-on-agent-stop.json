{
  "name": "KiroGraph: Sync when agent stops",
  "version": "1.0.0",
  "description": "Flushes dirty KiroGraph index when the agent finishes a task",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "runCommand",
    "command": "kirograph sync-if-dirty --quiet"
  }
}
